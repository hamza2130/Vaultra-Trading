export default function HomePage() {
  return (
    <div className="mx-auto max-w-[1240px] px-6 py-10">
      <h1 className="font-display text-xl font-extrabold text-text">Market overview</h1>
      <p className="mt-1 text-[13px] text-text-dim">
        Live coin prices and charts arrive in the next build phase — this page confirms the
        auth/KYC gate is wired up correctly.
      </p>
    </div>
  );
}
